#include <stdio.h>
/**
* main - Prints a random number and states whether
*it is positive, negative, or zero.
* Return: Always 0.
*/
int main(void)
{
char s = 'a';
while (s <= 'z')
{
putchar(s);
s++;
}
putchar('\n');
return (0);
}
