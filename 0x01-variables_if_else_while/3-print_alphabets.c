#include <stdio.h>
/**
* main - Prints a random number and states whether
* it is positive, negative, or zero.
* Return: Always 0.
*/
int main(void);
while (z <= 'z')
{
putchar(z);
z++;
}
z = 'A';
while (z <= 'Z')
{
putchar(z);
z++;
}
putchar('\n');
return (0);
}
